fastapi==0.100.0
uvicorn==0.22.0
pydantic==2.0.3
python-dotenv==1.0.0  # For environment variable management (if needed)
passlib==1.7.4        # For more secure password hashing (optional)
bcrypt==4.0.1         # For bcrypt password hashing (optional)
fastapi-cors
